import routes from './routes';

const config = {
    routes: routes.user,
    routesAdmin: routes.admin,
    routesSocial: routes.social,
    routesCompany: routes.pageCompany,
};

export default config;
